<p id="notice"><%= notice %></p>
<div class="container px-5 px-sm-0">
  <%= render "layouts/error.messages", validation: @book %>

  <div class="row">
    <!--side bar-->
    <div class="col-md-3">
      <%= render 'info', user: @user %>
      <%= render 'chat/chat', user: @user, room: @room, have_room: @have_room, user_room: @user_room %>
      <%= render 'btn', user: @user %>
      <%= render 'books/newbook', book: @book %>
    </div>
    <!--main-->
    <div class="col-md-8 offset-md-1">
      <%= render 'books/books', books: @books %>
    </div>
  </div>
</div>